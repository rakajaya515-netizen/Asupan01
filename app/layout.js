export const metadata = {
  title: "Asupanmu",
  description: "Streaming Video"
};

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  );
}
